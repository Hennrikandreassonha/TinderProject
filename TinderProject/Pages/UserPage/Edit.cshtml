@page
@model EditModel
@{
	ViewData["Title"] = "Edit profile";
}

<h1>@ViewData["Title"]</h1>

@if (!ViewData.ModelState.IsValid)
{
	<div class="alert alert-danger">
		<ul>
			@foreach (var key in ViewData.ModelState.Keys)
			{
				foreach (var error in ViewData.ModelState[key].Errors)
				{
					<li>@error.ErrorMessage</li>
				}
			}
		</ul>
	</div>
}

<div>
	<form method="post">
		<section>
			<label for="firstName">First name</label>
			<input type="text" id="firstName" asp-for="User.FirstName" />
			<span asp-validation-for="User.FirstName"></span>
		</section>
		<section>
			<label for="lastName">Last name</label>
			<input type="text" id="lastName" asp-for="User.LastName" />
			<span asp-validation-for="User.LastName"></span>
		</section>
		<section>
			<label for="dateOfBirth">Date of birth</label>
			<input type="datetime" id="dateOfBirth" asp-for="User.DateOfBirth" />
			<span asp-validation-for="User.DateOfBirth"></span>
		</section>
		<section>
			<label for="gender">Gender</label>
			<input type="text" id="gender" asp-for="User.Gender" />
			<span asp-validation-for="User.Gender"></span>
		</section>
		<section>
			<label for="preference">Preference</label>
			<input type="text" id="preference" asp-for="User.Preference" />
			<span asp-validation-for="User.Preference"></span>
		</section>
		<section>
			<label for="description">Description</label>
			<input type="text" id="description" asp-for="User.Description" />
			<span asp-validation-for="User.Description"></span>
		</section>
		<button type="submit">Update profile</button>
	</form>
</div>